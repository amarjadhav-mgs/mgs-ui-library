---
'@mgs/ui': minor
---

Add `Avatar` and `AvatarGroup` (RSuite 6) with their props types.

The MGS theme makes default avatar initials readable in light mode: RSuite puts white text on light gray (1.4:1);
the theme uses a darker gray (4.8:1). Dark and high-contrast themes are unchanged.
