---
'@mgs/ui': minor
---

Add `Input`, `InputGroup`, `Textarea` and `PasswordInput` (RSuite 6), with their props types.

The MGS theme darkens input borders so fields meet WCAG 1.4.11 contrast (3:1): RSuite's default is 1.3:1 in light
mode and 1.6:1 in dark mode; high contrast is unchanged.
