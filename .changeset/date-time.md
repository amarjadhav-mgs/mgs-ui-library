---
'@mgs/ui': minor
---

Add the date and time components (RSuite 6): `Calendar`, `DateInput`, `DatePicker`, `DateRangeInput`,
`DateRangePicker`, `TimePicker` and `TimeRangePicker`, with their props types, the `DateRange` type and the
DateRangePicker date rules (`beforeToday`, `afterToday`, `before`, `after`, `allowedDays`, `allowedMaxDays`,
`allowedRange`, `combine`).
